# NIGHT SIGNAL

Daily public intelligence issue.

Canonical operating guide:

- `README-night-signal.md`

Design source of truth:

- `docs/night-signal-basic-design.md`

Only `site/` is published. Private browser exports, password CSVs, payment-card
JSON, history JSON, and local `.env` files must stay out of commits.
